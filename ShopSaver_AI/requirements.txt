# General dependencies
streamlit>=1.30.0  # More recent Streamlit version
pandas>=2.1.0      # More recent Pandas version
numpy>=1.26.0      # More recent NumPy version, as suggested by the error log

# Other dependencies (allowing pip to select recent compatible versions)
scikit-learn
xgboost
shap
scipy

# Ensure pip and setuptools are modern for Python 3.12+ compatibility
setuptools>=65.5.1
pip>=23.0
